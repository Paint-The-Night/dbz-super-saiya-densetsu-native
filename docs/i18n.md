# Native i18n plan (Super Saiya Densetsu)

## Policy

- **No runtime ROM patching.** English and Japanese both boot the same clean
  Japanese Rev 1 image (SHA-256 gate unchanged).
- Language is chosen on the in-game **LANGUAGE / 言語** boot menu
  (`src/web_main.c`), which calls `dbz_i18n_set` before the emulator starts.
- Until text/font hooks + EN glyph tiles land, **in-game dialogue remains
  Japanese** when EN is selected. That is intentional: it avoids mojibake from
  natives walking JP glyph pipelines over IPS-patched script/font bytes.
- Klepto Software English IPS may be used as **reference wording only** under
  `research/klepto-reference/`. Never apply it in the product path.
- Tooling: `tools/extract_script_strings.py` dumps JP pointers/strings and can
  align Klepto EN bytes offline (`--klepto`).

## Current scaffold

| API | Role |
|-----|------|
| `DbzLang` (`DBZ_LANG_JA` / `DBZ_LANG_EN`) | Active language |
| `dbz_i18n_set` / `dbz_i18n_get` | Set from `lang_confirm` (C) |
| `dbz_i18n_str(id)` | Host/boot UI string table placeholders |
| `dbz_i18n_filter_script_byte(addr,b)` | Hook at `01:CFB4`; **identity under JA** |
| `dbz_i18n_en_script(bank_sel, idx, &len)` | Raw EN glyph bytes for early lines (C tables) |
| Optional `dbz_i18n_remap_script_byte` | Experimental hook pointer (compile-time off) |

Files: `src/i18n.h`, `src/i18n.c`.

## Honest EN ship state

1. Menu remains.
2. Selecting **ENGLISH** → `dbz_i18n_set(DBZ_LANG_EN)` → boot **clean** ROM.
3. Status may briefly show: `English: native translation layer in progress`.
4. Game dialogue stays Japanese until pointer-swap + EN font upload land.
5. C tables hold Klepto-encoded intro lines 0–2 for far-table bank sel `$0733==2`
   (main script at `06:8000`); filter does **not** rewrite them yet because EN
   lengths differ from JP (would desync the stream).

## Text pipeline (evidence)

### Not dialogue

| PC / range | Notes |
|------------|-------|
| **05:DEC3–DF5F** | Actor **script walker** opcodes `00/03/01/22` on `[$00]` via `$002C/$0030,Y`. Native: `actor_script_dec3_step`. **Not** the dialogue print leaf. |
| **05:CAFA** jump table | Actor/face/slot ops; adjacent to stream setup, not glyph text. |
| **00:8732** | Generic CPU→VRAM word copy (many consumers). |

### Dialogue decoder (bank `$01`)

| PC / RAM | Notes | Source |
|----------|-------|--------|
| **01:CF3F–CF66** | Resolve message: `$0733` ×3 indexes far table **`07:8F16`**; `$0723` ASL indexes u16 pointer table; result → DP+`$00` long. | ROM disasm |
| **07:8F16** | 24-bit bank table; **index 2 = `06:8000`** (main script pointers). | ROM `$38F16` |
| **06:8000 / file `$30000`** | **366** u16 LE string pointers; strings from **`06:82DC`** (~file `$302DC`) through ~`$35108`. | ROM + RHDN notes |
| **01:CFB2–CFC3** | Fetch loop head: `LDX $20` / **`LDA [$00],Y`** / `STA $04` / `INY` / `CMP #$01` → gap via `JSL $01D00B`, else fall to opcode dispatch at `$CFC4`. **Native:** `text_script_cfb2_step` + `dbz_i18n_filter_script_byte`. | ROM + native |
| **01:CFC4+** | Control dispatch: `FF`/`FE`/`FD`/`FC`/`F6+`/`F4`→`$D293`; `$A0–$DF` via `JSL $01BC88`; glyph emit `STA $7E3000,X`. | ROM |
| **01:D000 / D00B** | Write glyph tile pair to **`$7E3000,X`** (attr `#$24`); `D00B` shared INC `$071D`/`$071F` / RTL (also space gap). Native covers `D00B–D012`. | ROM + native |
| **`$7E3000,X`** | Dialogue **glyph word buffer** (also reused by map pattern fills elsewhere). | ROM + ram-map |
| **`$071D` / `$071F` / `$0721`** | Text cursor / position counters around CF80–CFA0. | ROM |
| **`$0723` / `$0733`** | String index / far-table bank selector. | ROM |

### Stream encoding (main script)

- Custom 1-byte glyph indices (not ASCII / Shift-JIS).
- **`F4 <face> 7E`** — open box / speaker.
- **`FF`** line, **`FE`** page, **`7F`** end box, **`01`** gap (CFB9 path).
- High bytes (`$A0+`, `$C8–$CC`, `$E0+`) take multi-byte / table expand paths.
- Klepto EN keeps the same control framing but retargets glyph indices and
  inserts font data into formerly empty **bank `$91`** (file `$088000`, JP=`FF`).

### Font / VRAM

| Item | Notes |
|------|-------|
| JP font | Compressed graphics (see RHDN thread / LotSS editor); not yet extracted into C. |
| Klepto EN font | IPS cluster ~file `$088000` (bank `$91`) + other GFX diffs — **reference only**. |
| Upload paths | `00:8732` VRAM CPU copy; `00:0800` DMA queue — glyphs likely enqueue after `$7E3000` decode. |

## Hook status this pass

| Piece | Status |
|-------|--------|
| Boot UI strings | Done (`web_main` + `dbz_i18n_str`) |
| `01:CFB2` fetch native + JA equivalence tests | **Landed** |
| `dbz_i18n_filter_script_byte` | Landed (JA identity; EN no rewrite yet) |
| EN script tables lines 0–2 | Landed in `i18n.c` (raw Klepto bytes) |
| Pointer-swap on message open (`CF3F`) | **Todo** (needed: EN lengths ≠ JP) |
| EN font tile upload when `lang==EN` | **Todo** |
| Full 366-string UTF-8 / glyph tables | **Todo** (use extract tool + Klepto ref) |
| Menu / battle string banks (`07:8F2E` etc.) | **Todo** |

## Next concrete steps

1. Nativize **01:CF3F–CF66** (or post-resolve at `STA $00`) so EN can retarget
   DP+`$00` to a WRAM/C-backed stream for `dbz_i18n_en_script` hits.
2. Provide a safe WRAM scratch (evidence-backed) or bus overlay for EN bytes
   without mutating the loaded ROM image.
3. Extract / port EN font tiles; upload when `dbz_i18n_get()==EN`.
4. Expand C tables beyond intro lines; keep JA path untouched.
5. Trace `tests/flight-event.inputs` with `DBZ_ENTRY_TRACE` around `01:CFB2`
   to confirm `$0733/$0723` for early Raditz/event lines.

## Research credits

- Script layout cross-check: romhacking.net thread discussion of LotSS text
  (pointers at `$30000`, body ~`$302DC`, uncompressed script).
- English reference wording: Klepto Software patch 1.02 (IPS not applied at
  runtime).
