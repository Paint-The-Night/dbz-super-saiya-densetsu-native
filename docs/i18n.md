# Native i18n plan (Super Saiya Densetsu)

## Policy

- **No runtime ROM patching.** English and Japanese both boot the same clean
  Japanese Rev 1 image (SHA-256 gate unchanged).
- Language is chosen on the in-game **LANGUAGE / 言語** boot menu
  (`src/web_main.c`), which calls `dbz_i18n_set` before the emulator starts.
- Until text/font hooks exist, **in-game dialogue remains Japanese** when EN is
  selected. That is intentional: it avoids mojibake from natives walking JP
  glyph pipelines over IPS-patched script/font bytes.
- Klepto Software English IPS may be used as **reference wording only** under
  `research/klepto-reference/`. Never apply it in the product path.

## Current scaffold

| API | Role |
|-----|------|
| `DbzLang` (`DBZ_LANG_JA` / `DBZ_LANG_EN`) | Active language |
| `dbz_i18n_set` / `dbz_i18n_get` | Set from `lang_confirm` (C) |
| `dbz_i18n_str(id)` | Host/boot UI string table placeholders |
| Optional `dbz_i18n_remap_script_byte` | Experimental hook pointer (compile-time off) |

Files: `src/i18n.h`, `src/i18n.c`.

## Honest EN ship state

1. Menu remains.
2. Selecting **ENGLISH** → `dbz_i18n_set(DBZ_LANG_EN)` → boot **clean** ROM.
3. Status may briefly show: `English: native translation layer in progress`.
4. Game dialogue stays Japanese until hooks + EN tables land.

## Hook research (next pass)

Recovered natives / RAM map candidates related to script streams and VRAM text
upload (not yet proven as the dialogue print leaf):

| PC / range | Notes | Source |
|------------|-------|--------|
| **05:DEC3–DF5F** | Actor **script walker** on `[$00]` via `$002C/$0030,Y`; opcodes; shared advance leaf `$DF51`. Strong candidate for script-byte consumption. Native: `actor_script_dec3_step`. | `docs/progress.md`, `native_video.inc` |
| **05:DF51** | Shared script-cursor advance leaf. | progress / DEC3 block |
| **05:CA10–CA88** | Actor slot update calling `$CA90/$DEC3/$DA64`. | progress |
| **05:BECB–C010** | Stream / VRAM-queue seed + WMDATA opcode walk — graphics stream, possible adjacent text path. Native: `actor_stream_becb_step`. | progress, ram-map |
| **00:8732–877D** | CPU word copy from `[DP+$00]` into **VRAM** (generic upload). Native: `vram_cpu_copy_step`. | progress, ram-map |
| **00:AE38–AE69** | Mode-7 / ch4 VRAM DMA from `$7E9000`. | progress |
| **00:0800 queue** | Eight-byte VRAM DMA queue entries (dest + size + source). Used by many upload paths; dialogue glyphs likely enqueue here once decoded. | ram-map |
| DP+$04..$07 @ 8732 | VRAM CPU-copy remainder / dest word address. | ram-map |
| `$002C,Y` / `$0030,Y` / `$002D,Y` | Script gate / cursor / timer for DEC3. | ram-map |

**Next concrete steps**

1. Trace an event-dialogue replay (`tests/flight-event.inputs`) with
   `DBZ_ENTRY_TRACE` around `05:DEC3` / print suspects; log script bytes read
   vs on-screen glyphs.
2. Identify the leaf that maps one script byte → glyph tile index / VRAM dest.
3. Maintain EN string tables in C; hook that leaf when `dbz_i18n_get()==EN`
   (function pointer or `#if` stub — see `DBZ_I18N_EXPERIMENTAL_HOOKS`).
4. Optionally borrow Klepto wording into tables; never IPS-apply.

No experimental hook is enabled in this ship — only the scaffold and docs.
